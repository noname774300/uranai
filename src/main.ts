import "crisp-game-lib";

const title = "";

const description = `
`;

const characters: string[] = [];

function update() {
  if (!ticks) {
    return;
  }
}

init({
  update,
  title,
  description,
  characters,
  options: {},
});
